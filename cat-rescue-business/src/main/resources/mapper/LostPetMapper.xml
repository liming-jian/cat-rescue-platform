<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaoxing.business.mapper.LostPetMapper">

    <select id="findAll" resultType="com.miaoxing.business.entity.LostPet">
        select id, pet_name, status, description, contact, image_url, publish_date
        from lost_pets
        order by id desc
    </select>

    <insert id="insert" parameterType="com.miaoxing.business.entity.LostPet" useGeneratedKeys="true" keyProperty="id">
        insert into lost_pets (pet_name, status, description, contact, image_url)
        values (#{petName}, #{status}, #{description}, #{contact}, #{imageUrl})
    </insert>

    <update id="updateStatus">
        update lost_pets
        set status = #{status}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from lost_pets where id = #{id}
    </delete>
</mapper>
