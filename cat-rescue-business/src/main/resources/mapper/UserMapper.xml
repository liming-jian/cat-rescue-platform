<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaoxing.business.mapper.UserMapper">

    <select id="findByUsername" resultType="com.miaoxing.business.entity.User">
        select id, username, password, role, created_at
        from users
        where username = #{username}
        limit 1
    </select>

    <select id="findByUsernameAndPassword" resultType="com.miaoxing.business.entity.User">
        select id, username, password, role, created_at
        from users
        where username = #{username}
          and password = #{password}
        limit 1
    </select>

    <insert id="insertUser" parameterType="com.miaoxing.business.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into users (username, password, role)
        values (#{username}, #{password}, #{role})
    </insert>

    <update id="updatePassword">
        update users
        set password = #{password}
        where username = #{username}
    </update>
</mapper>
