<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaoxing.business.mapper.CatMapper">

    <select id="findAll" resultType="com.miaoxing.business.entity.Cat">
        select id, name, age, gender, status, description, image_url, created_at
        from cats
        order by id desc
    </select>

    <select id="findByConditions" resultType="com.miaoxing.business.entity.Cat">
        select id, name, age, gender, status, description, image_url, created_at
        from cats
        <where>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="gender != null and gender != ''">
                and gender = #{gender}
            </if>
            <if test="ageKeyword != null and ageKeyword != ''">
                and age like concat('%', #{ageKeyword}, '%')
            </if>
            <if test="nameKeyword != null and nameKeyword != ''">
                and name like concat('%', #{nameKeyword}, '%')
            </if>
        </where>
        order by id desc
    </select>

    <insert id="insert" parameterType="com.miaoxing.business.entity.Cat" useGeneratedKeys="true" keyProperty="id">
        insert into cats (name, status, description, image_url, age, gender)
        values (#{name}, #{status}, #{description}, #{imageUrl}, #{age}, #{gender})
    </insert>

    <update id="updateStatus">
        update cats
        set status = #{status}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from cats where id = #{id}
    </delete>
</mapper>
